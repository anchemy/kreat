<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">

    {% include "components/head.njk" %}

    <body>

        <!-- Jellyfish background container -->
        <div id="jsi-jellyfish-container" class="container"></div>

        <div class="flex-grid {% if page.url == '/' %}{% else %}flex-grid--sb{% endif %} flex-break p-60 m-40 w-100">
            {% include "components/nav.njk" %}
        </div>

        {{ layoutContent | safe }}

        {% include "components/footer.njk" %}

    </body>

</html>